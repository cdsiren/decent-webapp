import React from 'react';
import { Widget } from '@typeform/embed-react'

const Waitlist = () => {
        return (
            <div>
                <Widget id="<Xkh00RVd>" style={{ width: '50%' }} className="my-form" />
            </div>
        );
}

export default Waitlist
